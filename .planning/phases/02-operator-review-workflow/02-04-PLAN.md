---
phase: 02-operator-review-workflow
plan: "04"
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - app/components/publisher/PublisherIntakeWorkspace.tsx
  - app/components/publisher/PublisherIntakeReviewWorkspace.tsx
  - app/lib/publisher/contracts.ts
  - app/lib/publisher/intake-ui.ts
  - app/lib/publisher/publisher.spec.ts
  - README.md
autonomous: true
requirements: [CONT-04, CONT-05, OPER-03, AGNT-03]
user_setup: []
must_haves:
  truths:
    - Constrained block editing exists for operator-safe fields and respects publisher ownership boundaries.
    - Invalid composition prevention is visible in the editing UX rather than only surfacing after the fact.
    - Risky or reserved fields explain why they are not editable through block editing.
  artifacts:
    - path: app/components/publisher/PublisherIntakeWorkspace.tsx
      provides: Operator entry points for constrained block editing
    - path: app/components/publisher/PublisherIntakeReviewWorkspace.tsx
      provides: Read-only explanations and validation cues for risky block fields
    - path: README.md
      provides: Documentation of constrained block editing and invalid composition prevention
  key_links:
    - from: app/components/publisher/PublisherIntakeReviewWorkspace.tsx
      to: app/lib/publisher/contracts.ts
      ensures: UI editing constraints match runtime contract boundaries
    - from: app/lib/publisher/intake-ui.ts
      to: app/components/publisher/PublisherIntakeWorkspace.tsx
      ensures: Editing affordances are driven by diagnostics/contract-safe context
---

<objective>
Finish constrained block editing UX for risky fields and invalid composition prevention.

Purpose: Let operators adjust safe block content without crossing the ownership rules or silently creating invalid compositions.
Output: Contract-safe editing cues, clearer prevention UX, and documentation/tests for the constrained editing model.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-operator-review-workflow/02-CONTEXT.md
@.planning/phases/02-operator-review-workflow/02-RESEARCH.md
@app/components/publisher/PublisherIntakeWorkspace.tsx
@app/components/publisher/PublisherIntakeReviewWorkspace.tsx
@app/lib/publisher/contracts.ts
@app/lib/publisher/intake-ui.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose constrained block editing paths for safe fields</name>
  <files>app/components/publisher/PublisherIntakeWorkspace.tsx, app/components/publisher/PublisherIntakeReviewWorkspace.tsx</files>
  <read_first>app/components/publisher/PublisherIntakeWorkspace.tsx, app/components/publisher/PublisherIntakeReviewWorkspace.tsx, app/lib/publisher/contracts.ts</read_first>
  <action>Add or tighten UI entry points for block-level editing so operators can change safe block props while staying inside current zone and ownership constraints. Keep risky or reserved fields visibly non-editable.</action>
  <verify>pnpm vitest app/lib/publisher/publisher.spec.ts</verify>
  <acceptance_criteria>
    - UI distinguishes editable safe props from reserved or risky concerns
    - Invalid composition or forbidden edits are surfaced before save/apply trust is assumed
    - Block editing remains scoped to Publisher Mode workflow
  </acceptance_criteria>
  <done>Operators can make safe block edits without crossing contract boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Explain invalid composition prevention in UI helpers</name>
  <files>app/lib/publisher/intake-ui.ts, app/lib/publisher/contracts.ts</files>
  <read_first>app/lib/publisher/intake-ui.ts, app/lib/publisher/contracts.ts, app/lib/publisher/checker.ts</read_first>
  <action>Provide helper logic or text mapping that explains why certain block edits are forbidden or risky, using the existing contract and diagnostics model. The operator should understand whether a field is blocked due to metadata ownership, zone rules, or composition validity.</action>
  <verify>pnpm vitest app/lib/publisher/publisher.spec.ts</verify>
  <acceptance_criteria>
    - intake-ui or adjacent helpers can distinguish ownership vs composition restrictions
    - UI copy reflects runtime contract boundaries accurately
    - Invalid composition prevention is visible before release-stage failure when possible
  </acceptance_criteria>
  <done>Constrained editing rules are explained instead of feeling arbitrary.</done>
</task>

<task type="auto">
  <name>Task 3: Document and test constrained editing behavior</name>
  <files>README.md, app/lib/publisher/publisher.spec.ts</files>
  <read_first>README.md, app/lib/publisher/publisher.spec.ts, app/lib/publisher/contracts.ts</read_first>
  <action>Document how constrained block editing works in Publisher Mode and add or update regression tests that cover reserved/risky editing boundaries where practical.</action>
  <verify>rg -n \"block editing|reserved|ownership|invalid composition\" README.md && pnpm vitest app/lib/publisher/publisher.spec.ts</verify>
  <acceptance_criteria>
    - README mentions constrained block editing or invalid composition prevention
    - Tests cover at least one reserved/risky editing boundary
    - pnpm vitest app/lib/publisher/publisher.spec.ts exits 0
  </acceptance_criteria>
  <done>Constrained editing behavior is documented and less likely to regress.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest app/lib/publisher/publisher.spec.ts`
- [ ] `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify plan-structure .planning/phases/02-operator-review-workflow/02-04-PLAN.md`
- [ ] Read editing UX and confirm reserved/risky fields explain why they are blocked
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Constrained block editing is visible and contract-safe
- Invalid composition prevention is understandable to operators
</success_criteria>

<output>
After completion, create `.planning/phases/02-operator-review-workflow/02-04-SUMMARY.md`
</output>
