---
phase: 02-operator-review-workflow
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - app/components/publisher/PublisherIntakeWorkspace.tsx
  - app/components/publisher/PublisherReleaseWorkspace.tsx
  - app/components/workbench/StructureView.tsx
  - app/lib/publisher/intake-ui.ts
  - app/lib/publisher/checker.ts
  - app/lib/publisher/publisher.spec.ts
autonomous: true
requirements: [OPER-02, OPER-03]
user_setup: []
must_haves:
  truths:
    - Operator can compare source, contract, and output through purpose-built review surfaces.
    - Diagnostics are grouped in operator-meaningful categories and severities.
    - Working and release issues remain distinct, with actionable drill-down to the right page or artifact.
  artifacts:
    - path: app/components/publisher/PublisherIntakeWorkspace.tsx
      provides: Workflow-level source/contract/output comparison framing
    - path: app/components/publisher/PublisherReleaseWorkspace.tsx
      provides: Grouped release and working diagnostics with clearer drill-down
    - path: app/lib/publisher/intake-ui.ts
      provides: Diagnostics-friendly categorization and labeling support
  key_links:
    - from: app/lib/publisher/checker.ts
      to: app/components/publisher/PublisherReleaseWorkspace.tsx
      ensures: Runtime diagnostics reach operators in grouped, readable form
    - from: app/components/workbench/StructureView.tsx
      to: publisher review surfaces
      ensures: Source, contracts, and output remain navigable from one operator workflow
---

<objective>
Add source -> contract -> output diff surfaces and grouped diagnostics by severity.

Purpose: Make operator review actionable and comparative instead of relying on raw files or scattered warnings.
Output: Better comparison framing, grouped diagnostics, and regression coverage for operator-facing diagnostics semantics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-operator-review-workflow/02-CONTEXT.md
@.planning/phases/02-operator-review-workflow/02-RESEARCH.md
@app/components/publisher/PublisherIntakeWorkspace.tsx
@app/components/publisher/PublisherReleaseWorkspace.tsx
@app/components/workbench/StructureView.tsx
@app/lib/publisher/intake-ui.ts
@app/lib/publisher/checker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen source-contract-output comparison framing</name>
  <files>app/components/publisher/PublisherIntakeWorkspace.tsx, app/components/workbench/StructureView.tsx</files>
  <read_first>app/components/publisher/PublisherIntakeWorkspace.tsx, app/components/workbench/StructureView.tsx, app/components/workbench/DiffView.tsx</read_first>
  <action>Use existing workbench/editor patterns to make source, contract, and output easier to compare from the publisher workflow. The operator should not need to manually hunt raw files to understand where a problem originated.</action>
  <verify>pnpm vitest app/lib/publisher/publisher.spec.ts</verify>
  <acceptance_criteria>
    - Publisher workflow exposes clearer source/contract/output comparison entry points
    - Existing workbench affordances are reused where practical instead of duplicating infrastructure
    - Comparison flow stays inside Publisher Mode context
  </acceptance_criteria>
  <done>Operator can inspect source, contracts, and output from one coherent workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Group diagnostics by severity and operator category</name>
  <files>app/components/publisher/PublisherReleaseWorkspace.tsx, app/lib/publisher/intake-ui.ts, app/lib/publisher/checker.ts</files>
  <read_first>app/components/publisher/PublisherReleaseWorkspace.tsx, app/lib/publisher/intake-ui.ts, app/lib/publisher/checker.ts, app/components/publisher/PublisherIntakeReviewWorkspace.tsx</read_first>
  <action>Improve diagnostics presentation so working/release issues are grouped by severity and by operator-meaningful categories such as metadata, composition, ownership, deprecated blocks, and output blockers. Use current diagnostic data rather than creating a second model.</action>
  <verify>pnpm vitest app/lib/publisher/publisher.spec.ts</verify>
  <acceptance_criteria>
    - Diagnostics UI distinguishes grouped categories beyond a flat check list
    - Working vs release severity remains obvious
    - Operator-facing messages point toward the page, zone, or artifact to inspect
  </acceptance_criteria>
  <done>Diagnostics become structured and reviewable for operators.</done>
</task>

<task type="auto">
  <name>Task 3: Cover diagnostics semantics with regression tests</name>
  <files>app/lib/publisher/publisher.spec.ts</files>
  <read_first>app/lib/publisher/publisher.spec.ts, app/lib/publisher/checker.ts, app/lib/publisher/intake-ui.ts</read_first>
  <action>Add targeted test coverage for grouped/operator-facing diagnostic semantics where practical, avoiding snapshot bloat. Focus on category/severity behavior and distinct working vs release failures.</action>
  <verify>pnpm vitest app/lib/publisher/publisher.spec.ts</verify>
  <acceptance_criteria>
    - Tests assert at least one grouped diagnostic or severity distinction
    - Existing checks remain compatible with release workspace behavior
    - pnpm vitest app/lib/publisher/publisher.spec.ts exits 0
  </acceptance_criteria>
  <done>Operator diagnostics structure is less likely to regress silently.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest app/lib/publisher/publisher.spec.ts`
- [ ] `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify plan-structure .planning/phases/02-operator-review-workflow/02-03-PLAN.md`
- [ ] Read comparison/diagnostic UI and confirm operators can tell source vs contract vs output problems apart
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Source, contracts, and output are easier to compare from one workflow
- Diagnostics are grouped clearly by severity and category
</success_criteria>

<output>
After completion, create `.planning/phases/02-operator-review-workflow/02-03-SUMMARY.md`
</output>
