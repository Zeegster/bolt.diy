---
phase: 02-operator-review-workflow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/publisher/status.ts
  - app/components/publisher/PublisherIntakeWorkspace.tsx
  - app/components/publisher/PublisherReleaseWorkspace.tsx
  - app/components/workbench/StructureView.tsx
  - app/lib/publisher/publisher.spec.ts
autonomous: true
requirements: [OPER-01, OPER-02]
user_setup: []
must_haves:
  truths:
    - Publisher workflow stages are visible as explicit operator states instead of implicit status strings.
    - Operator-facing status surfaces explain blocking conditions and next actions.
    - Workflow state remains derived from existing publisher state rather than a disconnected UI-only enum model.
  artifacts:
    - path: app/lib/publisher/status.ts
      provides: Clear stage and readiness derivation for intake/review/release workflow states
    - path: app/components/publisher/PublisherIntakeWorkspace.tsx
      provides: Operator-visible stage rail and next-action workflow framing
    - path: app/components/publisher/PublisherReleaseWorkspace.tsx
      provides: Release readiness surface aligned with explicit stage semantics
  key_links:
    - from: app/lib/publisher/status.ts
      to: app/components/workbench/StructureView.tsx
      ensures: Stage derivation flows from state logic into the workbench experience
    - from: app/components/publisher/PublisherIntakeWorkspace.tsx
      to: app/components/publisher/PublisherReleaseWorkspace.tsx
      ensures: Intake/review/release surfaces communicate one consistent workflow
---

<objective>
Stabilize intake state machine and visible stage/status indicators across publisher UI.

Purpose: Make the operator workflow legible before adding deeper repair and diff surfaces.
Output: Explicit stage presentation, clearer blocking-state messaging, and tests covering stage transitions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-operator-review-workflow/02-CONTEXT.md
@.planning/phases/02-operator-review-workflow/02-RESEARCH.md
@app/lib/publisher/status.ts
@app/components/publisher/PublisherIntakeWorkspace.tsx
@app/components/publisher/PublisherReleaseWorkspace.tsx
@app/components/workbench/StructureView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make workflow stages explicit in publisher status derivation</name>
  <files>app/lib/publisher/status.ts, app/components/workbench/StructureView.tsx</files>
  <read_first>app/lib/publisher/status.ts, app/components/workbench/StructureView.tsx, app/types/publisher.ts</read_first>
  <action>Refine publisher status derivation so intake review, contract-ready, release-ready, published, and failed transitions are explicit and stable. Expose enough workflow metadata for the UI to explain why a project is in its current stage and what blocks progression.</action>
  <verify>pnpm vitest app/lib/publisher/publisher.spec.ts</verify>
  <acceptance_criteria>
    - app/lib/publisher/status.ts exposes workflow-state logic beyond a bare string mapping
    - app/components/workbench/StructureView.tsx uses the refined workflow state instead of inventing separate stage heuristics inline
    - Stage transitions remain compatible with existing publisher state and build history data
  </acceptance_criteria>
  <done>Workflow stages are derived centrally and ready for operator-visible UI.</done>
</task>

<task type="auto">
  <name>Task 2: Surface stage progress and next actions in publisher workspaces</name>
  <files>app/components/publisher/PublisherIntakeWorkspace.tsx, app/components/publisher/PublisherReleaseWorkspace.tsx</files>
  <read_first>app/components/publisher/PublisherIntakeWorkspace.tsx, app/components/publisher/PublisherReleaseWorkspace.tsx, app/lib/publisher/status.ts</read_first>
  <action>Add explicit stage presentation and next-action guidance to intake/release surfaces so operators can see whether they are resolving intake ambiguity, repairing contracts, or validating release readiness. Keep the UI inside existing publisher workspace components.</action>
  <verify>pnpm vitest app/lib/publisher/publisher.spec.ts</verify>
  <acceptance_criteria>
    - PublisherIntakeWorkspace contains visible workflow stage framing beyond raw pass/warn/fail counts
    - PublisherReleaseWorkspace distinguishes stage progress from check severity
    - UI text points the operator toward the next corrective action when the project is blocked
  </acceptance_criteria>
  <done>Operator workflow stage and next-step guidance are visible across publisher UI.</done>
</task>

<task type="auto">
  <name>Task 3: Lock workflow-state behavior with regression coverage</name>
  <files>app/lib/publisher/publisher.spec.ts</files>
  <read_first>app/lib/publisher/publisher.spec.ts, app/lib/publisher/status.ts</read_first>
  <action>Add tests covering publisher workflow stage derivation for draft, intake-review, contract-ready, release-ready, and failed states using realistic combinations of intake session, checks, and build history.</action>
  <verify>pnpm vitest app/lib/publisher/publisher.spec.ts</verify>
  <acceptance_criteria>
    - Tests cover at least intake-review, contract-ready, release-ready, and failed stage outcomes
    - Stage derivation does not regress current release workspace expectations
    - pnpm vitest app/lib/publisher/publisher.spec.ts exits 0
  </acceptance_criteria>
  <done>Publisher workflow stage semantics are covered by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm vitest app/lib/publisher/publisher.spec.ts`
- [ ] `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify plan-structure .planning/phases/02-operator-review-workflow/02-01-PLAN.md`
- [ ] Read publisher stage UI and confirm it communicates workflow state and next steps clearly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Workflow stages are explicit and actionable in the operator UI
- Status derivation remains centralized and contract-safe
</success_criteria>

<output>
After completion, create `.planning/phases/02-operator-review-workflow/02-01-SUMMARY.md`
</output>
